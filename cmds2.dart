// fazendo importaçoes de bibliotecas
import 'dart:math';

main() {

 // o comando 'pow(numero,expoente);' serve para elevar um numero a um determinado expoente
 int n= pow(5,2);
 print(" 5 elevado a 2 é igual a $n");
  // usar o "/n " durante um comando ira quebrar a linha
}